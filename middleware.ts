// TODO: Implement Next.js auth middleware
// - Refresh the Supabase session on every request using the server client
// - Protect routes that require authentication (redirect to /auth/login if no session)
// - Define the matcher to exclude static assets and public routes
// - Must live at the project root (Next.js requirement)
